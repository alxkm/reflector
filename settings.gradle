plugins {
    // Lets Gradle download a JDK when the build asks for a toolchain it cannot find,
    // so the CI matrix can run the tests on Java 8 without installing it by hand.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'reflector'
